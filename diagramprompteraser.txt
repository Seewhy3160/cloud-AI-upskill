Create an AWS cloud architecture diagram for "StreamFlix — Multi-Region Streaming Platform". Show infrastructure topology using AWS architecture conventions: nested containers for AWS Cloud > Region > VPC > Availability Zone > Subnet, with AWS service icons placed inside the correct boundaries. Show TWO active-active regions side by side (Region A: primary, Region B: secondary).

GLOBAL / EDGE LAYER (outside the regions, at the top, spanning both):
- Route 53 (latency-based + geolocation routing, health-check failover)
- CloudFront CDN (global edge locations)
- AWS WAF + Shield
- AWS Certificate Manager
Connect Route 53 -> CloudFront -> regional ALBs.

EACH REGION contains one VPC. Inside each VPC show TWO Availability Zones, and inside each AZ a public subnet and a private subnet:

Public subnet (per AZ):
- Application Load Balancer
- API Gateway
- NAT Gateway

Private subnet (per AZ) — compute:
- Amazon EKS cluster (node groups) running microservices: API services (Node.js), Profiles + Recommendations, Payments, Search
- AWS Lambda (thumbnails, metadata, compliance checks)
- AWS Elemental MediaLive (RTMP ingest) + MediaConvert (transcoding/ABR)

Private subnet (per AZ) — data:
- Amazon Aurora PostgreSQL (primary in Region A with read replicas; cross-region replica in Region B)
- Amazon ElastiCache (Redis)
- Amazon OpenSearch
- Amazon Timestream (time-series)
- Amazon MSK (Apache Kafka)

REGIONAL MANAGED / STORAGE (region-scoped, shown at VPC edge):
- Amazon S3 (video object storage, intelligent tiering, lifecycle to Glacier for archive)
- Amazon Redshift (data warehouse)
- Amazon SageMaker (recommendation, moderation, metadata ML)

SECURITY & MANAGEMENT (side panel or bottom band):
- AWS KMS, AWS IAM, AWS Secrets Manager, Amazon Cognito (IdP + MFA)
- Amazon CloudWatch, AWS X-Ray, Prometheus/Grafana (observability)

CONNECTIVITY:
- VPC peering / Transit Gateway between Region A and Region B for cross-region replication
- VPC endpoints / PrivateLink from private subnets to S3, MSK, SageMaker
- Internet Gateway on each VPC; NAT Gateway for private subnet egress

Use official AWS service icons, AWS group/container styling (Region, VPC, AZ, subnet boundaries with their standard colors), and label each boundary. Layout: two regions side by side, edge layer on top, security/observability as a labeled band. This is an infrastructure topology diagram, not a logical flow diagram.